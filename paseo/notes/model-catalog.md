# Model catalog

Paseo's provider catalog controls selectable/default models and thinking options in Paseo. The launcher independently pins `grok-4.6` and `--reasoning-effort`.

Keep the Paseo provider model ID, overlay `model`, launcher flags, and `thinkingOptions` aligned.

