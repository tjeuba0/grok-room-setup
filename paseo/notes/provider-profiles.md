# Provider profiles

The live template defines four derived Grok providers. Their `command` launches `grok-room`, which selects an isolated runtime through `GROK_HOME`.

`models` is a replacement catalog for each custom profile. `additionalModels` would be additive; this setup does not currently use it.

