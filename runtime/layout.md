# Expected role layout

```text
<role>/
├── config.toml
├── agent-profile.md
├── auth.json          # copy of ~/.grok/auth.json
├── hooks/
└── hooks-paths
```

Supervisor additionally owns a durable `SUPERVISOR_NOTEBOOK.md` initialized from the workflow template.
